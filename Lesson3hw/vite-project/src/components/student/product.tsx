function Product({ name, lang }) {
    return (
        <div>
            <p>The students name is {name} and their first programming language was {lang}</p>
        </div>
    )
}
export default Product;